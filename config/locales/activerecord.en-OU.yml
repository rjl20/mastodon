---
en-OU:
  activerecord:
    attributes:
      status:
        text: Status
    errors:
      messages:
        record_invalid: 'Validation did not pass: %{errors}'
      models:
        account:
          attributes:
            username:
              invalid: 'Valid glyphs: A-D and F-Z (plus small glyphs), 0-9, and "_"'
        status:
          attributes:
            reblog:
              taken: of status already exists
        tag:
          attributes:
            name:
              invalid: activerecord.errors.models.tag.attributes.name.invalid
